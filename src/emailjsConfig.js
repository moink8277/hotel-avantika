/**
 * emailjsConfig.js
 * Single source of truth for EmailJS credentials, used by both the
 * Contact form and the Newsletter signup (same template, per project
 * decision — see handoff doc).
 *
 * Fill these in from your EmailJS dashboard:
 * - SERVICE_ID  -> Email Services tab
 * - TEMPLATE_ID -> Email Templates -> your template
 * - PUBLIC_KEY  -> Account -> General -> API Keys
 */
export const EMAILJS_SERVICE_ID = "service_1gw4gxe";
export const EMAILJS_TEMPLATE_ID = "template_ser4m1a";
export const EMAILJS_PUBLIC_KEY = "xrZ0xTS_F10vVqlNq";